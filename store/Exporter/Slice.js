import { createSlice } from "@reduxjs/toolkit";

export const ExporterSlice = createSlice({
    name: "Exporter",
    initialState: {
        activeData: []
    },
    reducers: {},
    extraReducers: (builder) => {
        builder
        
    }

})