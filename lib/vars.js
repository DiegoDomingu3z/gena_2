/**
 * Array of statuses of which the user will be allowed to edit their order, which
 * Includes:
 * * waiting for approval
 * * declined
 * * approved
 */
export const editableStatus = ["waiting for approval", "declined", "approved"];
